version: '3.8'

services:
  semantic-core:
    build: .
    volumes:
      - .:/app  # Монтируем весь код
      - /app/venv  # Исключаем venv если он есть
      - /app/__pycache__
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1 